<h2>Edit User</h2>

<%= form_for(:user, :url => {:controller => 'users', :action => 'update', :id => @user.id}, html: { multipart: true }) do |form| %>
	<%= render(:partial => "shared/user_form", :locals => {:form => form}) %>
<% end %>